<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WindowBouncer.Views.AboutWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Margin="32" VerticalAlignment="Center" Spacing="6">
            <TextBlock Text="WindowBouncer" FontSize="22" FontWeight="SemiBold"/>
            <TextBlock x:Name="VersionText" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            <TextBlock x:Name="BuildDateText" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            <TextBlock TextWrapping="Wrap"
                       Margin="0,8,0,16"
                       Text="Close open windows from the system tray."/>
            <Button HorizontalAlignment="Right" Width="80"
                    Content="OK" Click="OK_Click"
                    Style="{ThemeResource AccentButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
